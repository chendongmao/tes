# 数据共享接口文档

- 服务地址：http://10.11.3.100:2024
- 共 6 个接口

> 调用凭证（AppKey）不在本文件内，由平台管理员单独提供。
>
> YC4kO6FS8LnFdVSilUlO3KMU
>
> http://192.168.206.207:11024

## 接入说明

```text
调用方式
1. 每个请求带 AppKey 头即可调用。
2. 响应统一为 {code, message, data}，HTTP 恒 200，业务失败看 code。

错误码
  8002 应用已禁用
  8004 共享应用不存在（AppKey 缺失或查无此人）

```

## 数据权限

查询本账号被授权的设备与传感器范围

### 查询数据权限范围

`POST` `http://10.11.3.100:2024/api/open/share/permission`

返回本账号被授权的设备清单及其传感器，可按设备编码/业务类型再筛，分页返回。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 |  | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| pageNum |  | int64 | 是 | 页码(不能为空) |
| pageSize |  | int64 | 是 | 每页数量(不能为空) |
| searchCount |  | boolean | 否 | 是否查询总条数 |
| sortItemList |  | array[SortItem] | 否 | 排序字段集合 |
|  | isAsc | boolean | 是 | true正序\\|false倒序 |
|  | column | string | 是 | 排序字段 |
| deviceCodes |  | array[string] | 否 | 设备编码过滤（空=不限） |
| businessType |  | string | 否 | 业务类型过滤 = 业务分类编码（空=不限） |

**请求示例**

```json
{
  "pageNum": 1,
  "pageSize": 10,
  "searchCount": true,
  "sortItemList": [
    {
      "isAsc": true,
      "column": "string"
    }
  ],
  "deviceCodes": [
    "string"
  ],
  "businessType": "string"
}
```

**响应参数**

| 参数名 |  |  |  | 类型 | 说明 |
|---|---|---|---|---|---|
| code |  |  |  | int32 | 业务码，0=成功；非 0 见错误码表 |
| message |  |  |  | string | 提示信息（随请求语言本地化） |
| data |  |  |  | PageResultPermissionDeviceDto |  |
|  | pageNum |  |  | int64 | 当前页 |
|  | pageSize |  |  | int64 | 每页的数量 |
|  | total |  |  | int64 | 总记录数 |
|  | pages |  |  | int64 | 总页数 |
|  | list |  |  | array[PermissionDeviceDto] | 结果集 |
|  |  | deviceId |  | string | 设备 ID |
|  |  | deviceCode |  | string | 设备编码 |
|  |  | deviceName |  | string | 设备名称 |
|  |  | orgCode |  | string | 归属组织编码 |
|  |  | businessType |  | string | 业务类型（业务分类编码） |
|  |  | groupId |  | int64 | 设备分组 ID |
|  |  | groupName |  | string | 设备分组名称 |
|  |  | longitude |  | number | 经度 |
|  |  | latitude |  | number | 纬度 |
|  |  | coordinateSystem |  | string | 坐标系（WGS84/GCJ02/BD09 等）——经纬度脱离坐标系无法正确落图 |
|  |  | tags |  | object | 设备标签（键值对，来自设备扩展字段） |
|  |  | sensors |  | array[PermissionSensorDto] | 该设备的传感器清单 |
|  |  |  | deviceId | string | 设备 ID |
|  |  |  | sensorId | string | 传感器标识（= DECODE sensor_id） |
|  |  |  | sensorName | string | 传感器名称 |
|  |  |  | unit | string | 单位 |
|  |  |  | valueType | string | 值类型 |
|  |  |  | tag | string | 传感器标签（设备内标识） |
|  | emptyFlag |  |  | boolean | 是否为空 |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": {
    "pageNum": 0,
    "pageSize": 0,
    "total": 0,
    "pages": 0,
    "list": [
      {
        "deviceId": "string",
        "deviceCode": "string",
        "deviceName": "string",
        "orgCode": "string",
        "businessType": "string",
        "groupId": 0,
        "groupName": "string",
        "longitude": 0,
        "latitude": 0,
        "coordinateSystem": "string",
        "tags": {},
        "sensors": [
          {
            "deviceId": null,
            "sensorId": null,
            "sensorName": null,
            "unit": null,
            "valueType": null,
            "tag": null
          }
        ]
      }
    ],
    "emptyFlag": true
  }
}
```


## 传感器数据

实时值、历史明细、统计聚合与抄表读数

### 获取传感器统计数据

`POST` `http://10.11.3.100:2024/api/open/share/sensor/stat`

period=RAW 取明细，MINUTE/HOUR 按周期聚合；MIN_TIME/MAX_TIME 返回周期内极值发生时刻（epoch ms，置于 value）。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| deviceId | string | 是 | 设备 ID |
| sensorId | string | 是 | 传感器标识（= DECODE sensor_id） |
| period | string | 是 | 统计周期：RAW（明细）/ MINUTE / HOUR |
| aggFunction | string | 否 | 聚合函数（period≠RAW 时生效），不传默认 AVG：AVG/MAX/MIN/COUNT/SUM/DIFF/MIN_TIME/MAX_TIME |
| startTime | string | 是 | 起始时间 yyyy-MM-dd HH:mm:ss（含） |
| endTime | string | 是 | 结束时间 yyyy-MM-dd HH:mm:ss（含） |

**请求示例**

```json
{
  "deviceId": "string",
  "sensorId": "string",
  "period": "string",
  "aggFunction": "string",
  "startTime": "string",
  "endTime": "string"
}
```

**响应参数**

| 参数名 |  | 类型 | 说明 |
|---|---|---|---|
| code |  | int32 | 业务码，0=成功；非 0 见错误码表 |
| message |  | string | 提示信息（随请求语言本地化） |
| data |  | array[OpenSensorPoint] | 业务数据，失败时为 null |
|  | value | double | 数值 |
|  | time | int64 | 读数时间 epoch ms |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "value": 0,
      "time": 0
    }
  ]
}
```


### 获取传感器实时数据

`POST` `http://10.11.3.100:2024/api/open/share/sensor/realtime`

单设备取各传感器最新值；sensorIds 为空则取该设备全部传感器。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| deviceId | string | 是 | 设备 ID |
| sensorIds | array[string] | 否 | 传感器标识集合；空/不传 = 该设备全部传感器 |

**请求示例**

```json
{
  "deviceId": "string",
  "sensorIds": [
    "string"
  ]
}
```

**响应参数**

| 参数名 |  | 类型 | 说明 |
|---|---|---|---|
| code |  | int32 | 业务码，0=成功；非 0 见错误码表 |
| message |  | string | 提示信息（随请求语言本地化） |
| data |  | array[OpenSensorValue] | 业务数据，失败时为 null |
|  | deviceId | string | 设备 ID |
|  | sensorId | string | 传感器标识（= DECODE sensor_id） |
|  | value | double | 数值 |
|  | time | int64 | 读数时间 epoch ms |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": [
    {
      "deviceId": "string",
      "sensorId": "string",
      "value": 0,
      "time": 0
    }
  ]
}
```


### 获取水表抄表读数

`POST` `http://10.11.3.100:2024/api/open/share/sensor/meter-history`

按水表编号（= 设备业务编码）批量取时间范围内的读数，返回 meterNo 到读数列表的映射。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| meterNos | array[string] | 是 | 水表编号集合 = 设备业务编码，至少一个 |
| startTime | string | 是 | 起始时间 yyyy-MM-dd HH:mm:ss（含） |
| endTime | string | 是 | 结束时间 yyyy-MM-dd HH:mm:ss（含） |

**请求示例**

```json
{
  "meterNos": [
    "string"
  ],
  "startTime": "string",
  "endTime": "string"
}
```

**响应参数**

| 参数名 | 类型 | 说明 |
|---|---|---|
| code | int32 | 业务码，0=成功；非 0 见错误码表 |
| message | string | 提示信息（随请求语言本地化） |
| data | object | 业务数据，失败时为 null |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": {}
}
```


### 获取历史数据·单设备

`POST` `http://10.11.3.100:2024/api/open/share/sensor/history/multi-sensor`

时间范围内的明细数据，按 sensorId 分组，游标翻页（首页不传 cursor，后续回传上一页的 nextCursor）。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| deviceId | string | 是 | 设备 ID |
| sensorIds | array[string] | 否 | 传感器标识集合；空/不传 = 该设备全部传感器 |
| startTime | string | 是 | 起始时间 yyyy-MM-dd HH:mm:ss（含） |
| endTime | string | 是 | 结束时间 yyyy-MM-dd HH:mm:ss（含） |
| cursor | string | 否 | 游标：上一页返回的 nextCursor；空=取首页（最新一批） |
| pageSize | int32 | 否 | 每页条数（按扁平行计，非分组数）；空=默认 500 |

**请求示例**

```json
{
  "deviceId": "string",
  "sensorIds": [
    "string"
  ],
  "startTime": "string",
  "endTime": "string",
  "cursor": "string",
  "pageSize": 0
}
```

**响应参数**

| 参数名 |  | 类型 | 说明 |
|---|---|---|---|
| code |  | int32 | 业务码，0=成功；非 0 见错误码表 |
| message |  | string | 提示信息（随请求语言本地化） |
| data |  | OpenHistoryPage |  |
|  | groups | object | 数据点分组（组内按时间倒序） |
|  | nextCursor | string | 下一页游标（不透明串，原样回传）；无更多数据时为 null |
|  | hasMore | boolean | 是否可能还有下一页（本页取满 pageSize 即为 true） |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": {
    "groups": {},
    "nextCursor": "string",
    "hasMore": true
  }
}
```


### 获取历史数据·多设备

`POST` `http://10.11.3.100:2024/api/open/share/sensor/history/multi-device`

时间范围内的明细数据，按 deviceId 分组，游标翻页。

**请求头**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| AppKey | string | 是 | AppKey 公开标识（唯一鉴权头） |

**请求体字段**

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| deviceIds | array[string] | 是 | 设备 ID 集合，至少一个 |
| sensorId | string | 是 | 传感器标识（= DECODE sensor_id） |
| startTime | string | 是 | 起始时间 yyyy-MM-dd HH:mm:ss（含） |
| endTime | string | 是 | 结束时间 yyyy-MM-dd HH:mm:ss（含） |
| cursor | string | 否 | 游标：上一页返回的 nextCursor；空=取首页（最新一批） |
| pageSize | int32 | 否 | 每页条数（按扁平行计，非分组数）；空=默认 500 |

**请求示例**

```json
{
  "deviceIds": [
    "string"
  ],
  "sensorId": "string",
  "startTime": "string",
  "endTime": "string",
  "cursor": "string",
  "pageSize": 0
}
```

**响应参数**

| 参数名 |  | 类型 | 说明 |
|---|---|---|---|
| code |  | int32 | 业务码，0=成功；非 0 见错误码表 |
| message |  | string | 提示信息（随请求语言本地化） |
| data |  | OpenHistoryPage |  |
|  | groups | object | 数据点分组（组内按时间倒序） |
|  | nextCursor | string | 下一页游标（不透明串，原样回传）；无更多数据时为 null |
|  | hasMore | boolean | 是否可能还有下一页（本页取满 pageSize 即为 true） |

**响应示例**

```json
{
  "code": 0,
  "message": "string",
  "data": {
    "groups": {},
    "nextCursor": "string",
    "hasMore": true
  }
}
```

